pub ::
std::mem: mod {
    pub extern ::
    alloc(block_size: int): Ptr;

    pub extern ::
    dealloc(block: Ptr): void;

    pub extern ::
    copy(src_block: Ptr, dst_block: Ptr, block_size: int): void;
}
